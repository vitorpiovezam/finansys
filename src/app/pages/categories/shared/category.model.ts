export class Category {
    constructor (
        public id?: number,
        public name?: string,
        public description?: string
    ) {
        this.id = id;
        this.name = name;
        this.description = description;
    }

}
